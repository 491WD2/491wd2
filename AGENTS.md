# AGENTS.md

## Cursor Cloud specific instructions

This repository is a **GitHub profile README** repo (`491WD2/491wd2`). Its only purpose is that
`README.md` renders on the owner's GitHub profile page.

- There is **no application, service, backend, frontend, database, build step, or test suite**.
- There is **no package manager** (no `package.json`, `requirements.txt`, etc.), so there are no
  dependencies to install and nothing to lint/test/build/run.
- "Development" here means editing `README.md` (Markdown). Preview it with any Markdown renderer or
  by pushing and viewing the GitHub profile at https://github.com/491WD2.
- Note: the default template's content lives inside an HTML comment (`<!-- ... -->`), so only the
  `## Hi there 👋` heading renders until that comment is removed/edited.
- The committed `.gitignore` is Node/Vite-oriented, hinting a future JS app *may* be added. If a
  `package.json` ever appears, install deps with the matching package manager (lockfile decides).
